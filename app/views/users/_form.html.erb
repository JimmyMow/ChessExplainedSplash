<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field email-text-field-container">
    <%= f.email_field :email, placeholder: "bobbyfischer@gmail.com", autofocus:true, class: "email-text-field" %>
  </div>
  <div class="actions submit-container">
    <%= f.submit "Sign up", class: "submit-button" %>
  </div>
<% end %>
